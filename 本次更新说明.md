# 搜索与推荐体验更新

## 深度轮换搜索

- 普通刷新负责检查第 1–5 页。
- 开启深度轮换后，后台依次检查第 6–10、11–15、16–20……46–50 页。
- 检查完第 50 页后返回第 6–10 页，继续检查这些较深页面是否出现新商品或价格变化；不会创建额外的“第 51 页”。
- 建议轮换间隔不少于 300 秒。虽然允许设置为 1 秒，但频繁访问可能触发平台访问限制或账号验证。

## 本次修复

- 修复搜索结果缺少城市字段时，被城市条件错误过滤的问题。
- 兼容“九成新”和“9新”等不同成色写法。
- 搜索接口数据结构发生变化时，增加通用结果解析回退，降低“平台能搜到、软件里没有”的概率。
- 推荐页改为按条件横向切换，一次只展示当前条件对应的商品。
- 排序改为“最新发现”和“价格最低”两个统一风格的切换按钮。
- 顶部加入居中的绿色刷新进度条；更新时仅“正在更新”文字变绿。
- 保留原项目 MIT 许可证及源码声明。
# 搜索归零修复（2026-08-27）

- 移除会因闲鱼页面改版而让整轮搜索直接归零的“模拟点击地区/价格/成色”链路。
- 搜索接口同时合并旧版与新版商品卡片结构，不再识别到少量旧卡片后漏掉其余商品。
- 接口结构无法识别时，从闲鱼网页已渲染的商品卡片读取商品 ID、标题与价格兜底。
- 首屏最长等待由 5 秒增加到 15 秒；翻页同时通过接口和网页商品 ID 判断是否成功。
- 价格与固定九档成色在抓取后统一筛选；地区字段缺失时不把真实候选误删。

## 闲鱼原生搜索对账（2026-08-28）

- 搜索改为直接复用闲鱼 PC 的原生 mtop 请求，不再模拟点击价格和地区面板。
- 价格通过 `priceRange` 传入，地区通过 `divisionList` 传入，页大小固定为闲鱼的 30 件。
- 默认采用与网页一致的“综合”排序，不擅自切换成最新发布。
- 原生搜索结果不再被标题关键词和残缺地区字段二次删减。
- 推荐页默认显示“所有候选”，AI 精选改为用户主动切换，避免 11 件看起来只剩几件。
- 闲鱼报告总数不足一页时强制对账：例如返回 11 件，软件必须解析出 11 个唯一商品 ID；不一致则停止入库并明确报错，不展示半截结果。
