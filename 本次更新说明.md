# 搜索与推荐体验更新

## 闲鱼原生数量与地区对账修复（2026-08-29）

- 地区请求改用闲鱼 PC 搜索来源 `pcSearch`，恢复原生地区筛选要求的 `extraDivision` 字段。
- 增加全国省、市、区县归属校验；选择杭州后不再收进通州区、松江区商品。
- 型号按完整字母数字词匹配；`G1` 不再从 `AEG 1` 等不连续文字中误命中。
- 优先解析闲鱼 PC 卡片的 `p_city` 城市字段，同时兼容区县 `area` 字段。
- 闲鱼报告的总数在已抓页数覆盖范围内时执行逐件对账；98 件只解析到 2 件会停止入库并明确报错。
- 条件修改后再次运行，会把仍命中的待审商品立即同步到当前条件分类，不再残留旧分类名称。
- “所有候选”改为完整保存闲鱼原生搜索结果，不再用历史去重和本地关键词二次过滤把 98 件缩减成 0--2 件；AI/精准筛选只影响精选视图。
- 推荐页默认展示“精准匹配”：品牌词与独立型号必须同时出现；“闲鱼原生”保留全部候选用于数量对账，镜头、后背适配器、代购说明等不再占据默认结果。
- 精准匹配兼容只写“G1绿标/白标机身”的闲鱼标题、`CONTAX G1`、全角 `Ｇ１` 和常见品牌别名；仍排除镜头、转接器与 `AEG 1` 误命中。
- 每轮搜索同步更新数据库中的商品标题、链接、卖家和所属条件，避免历史旧标题导致真实商品持续被判为不匹配。
- 搜索主链改为使用闲鱼真实 PC 搜索页：由网页自身应用关键词、价格、省、市、区县和成色，再读取网页实际响应与真实翻页；不再自行拼接口参数。
- 任一网页筛选未成功生效时立即停止并明确报错，不再退回全国商品或扩展推荐冒充搜索成功。
- 捕获每一次闲鱼搜索响应时，同时读取该响应真正提交的关键词、价格和地区；只有三项与当前条件完全一致，才允许解析和入库，延迟返回的旧条件响应不再被误当成新结果。
- 每次真实翻页都重新校验请求条件，避免第一页正确、后续页面退回全国或旧关键词。
- 搜索失败原因改为长期显示在主页面顶部，不再只出现几秒钟后消失；可直接看到失败发生在价格、地区、成色、翻页还是商品解析阶段。
- 增加页面真实请求条件回归测试；本版搜索、关键词、地区、存储及运行服务共 59 项测试通过。

## 深度轮换搜索

- 普通刷新负责检查第 1–5 页。
- 开启深度轮换后，后台依次检查第 6–10、11–15、16–20……46–50 页。
- 检查完第 50 页后返回第 6–10 页，继续检查这些较深页面是否出现新商品或价格变化；不会创建额外的“第 51 页”。
- 建议轮换间隔不少于 300 秒。虽然允许设置为 1 秒，但频繁访问可能触发平台访问限制或账号验证。

## 本次修复

- 修复搜索结果缺少城市字段时，被城市条件错误过滤的问题。
- 兼容“九成新”和“9新”等不同成色写法。
- 搜索接口数据结构发生变化时，增加通用结果解析回退，降低“平台能搜到、软件里没有”的概率。
- 推荐页改为按条件横向切换，一次只展示当前条件对应的商品。
- 排序改为“最新发现”和“价格最低”两个统一风格的切换按钮。
- 顶部加入居中的绿色刷新进度条；更新时仅“正在更新”文字变绿。
- 保留原项目 MIT 许可证及源码声明。
# 搜索归零修复（2026-08-27）

- 移除会因闲鱼页面改版而让整轮搜索直接归零的“模拟点击地区/价格/成色”链路。
- 搜索接口同时合并旧版与新版商品卡片结构，不再识别到少量旧卡片后漏掉其余商品。
- 接口结构无法识别时，从闲鱼网页已渲染的商品卡片读取商品 ID、标题与价格兜底。
- 首屏最长等待由 5 秒增加到 15 秒；翻页同时通过接口和网页商品 ID 判断是否成功。
- 价格与固定九档成色在抓取后统一筛选；地区字段缺失时不把真实候选误删。

## 闲鱼原生搜索对账（2026-08-28）

- 搜索改为在已登录页面环境中调用闲鱼网页自己的 mtop 方法，由网页完成签名；不再模拟点击容易失效的地区弹层。
- 价格、浙江/杭州、区县和页码直接交给闲鱼服务端筛选，并补齐 `xyHome`、`SEARCH_TAB_MAIN`、`pc` 等当前网页请求字段。
- 修复筛选操作后才清空响应的竞态问题，以及“浙江全省”的中间响应被误当成“杭州”最终响应的问题。
- 搜索结果改为接口与网页卡片双通道读取：账号页面复用缓存、未再次触发接口时，直接读取网页真实卡片，不再误报“网页没有返回搜索结果”。
- 价格、地区和翻页同时核对商品 ID 变化；接口与页面都未变化时才停止并报错。
- 默认采用与网页一致的“综合”排序，不擅自切换成最新发布。
- 地区改为省/市/区三级保存与请求；只填写杭州时自动识别为浙江/杭州。
- 修复旧版把“上城区”错误传入城市字段、同时省份为空，导致平台忽略地区并返回全国商品的问题。
- 原生结果仍要求品牌和型号全部命中，剔除平台扩展推荐中的其他型号、镜头、转接环和配件。
- 增加“适用/可用/兼容”、G45/2 等镜头规格、KITH 联名及服饰周边识别，避免标题末尾提到 G1 就被当成 G1 相机。
- 商品卡片缺少城市字段时不误删；明确返回其他区县时才排除。
- 推荐页默认显示“所有候选”，AI 精选改为用户主动切换，避免 11 件看起来只剩几件。
- 闲鱼报告总数不足一页时强制对账：例如返回 11 件，软件必须解析出 11 个唯一商品 ID；不一致则停止入库并明确报错，不展示半截结果。
- 升级时自动清理旧搜索规则留下的待审商品和实时事件，避免旧的错型号继续残留。

## 全国省市区三级联动（2026-08-28）

- 地区不再允许手动输入，改为“省份 → 城市 → 区县”三级下拉选择。
- 内置全国 34 个省级地区、对应城市与区县，运行时无需联网加载地区列表。
- 更改省份会自动清空旧城市和区县；更改城市会自动清空旧区县，避免残留条件串到下一次搜索。
- 直辖市也按三级方式显示，例如“北京 → 北京 → 朝阳区”。
- 搜索请求只使用闲鱼 PC 搜索识别的 `province`、`city`、`area` 与 `divisionList`；移除非原生的 `extraDivision` 字段。
- 保留闲鱼网页端的关键词纠错能力，价格、地区和页码继续交给闲鱼服务端筛选。
- 修复 `.gitignore` 把 `frontend/src/data/china-regions.json` 误当作运行数据忽略的问题；全国区划文件现在会正常提交到 GitHub，避免 Windows/macOS 同时在“构建前端”阶段失败。
