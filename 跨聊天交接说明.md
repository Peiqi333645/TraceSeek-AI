# 寻迹AI助手（TraceSeek AI）跨聊天交接说明

## 如何在另一个 ChatGPT 账号继续

1. 将整个 `TraceSeek-AI-CrossChat-Handoff.zip` 上传到新聊天。
2. 对新聊天说：`请先完整阅读“跨聊天交接说明.md”和“AGENTS.md”，然后检查源码，继续开发。不要重新创建项目。`
3. 如需生成安装包，请让新聊天修改源码后，再通过本项目的 GitHub Actions 构建 Windows 和 Mac 安装包。

## 项目定位

- 中文名称：寻迹AI助手
- 英文名称：TraceSeek AI
- 用途：闲鱼商品推荐、收藏监控、降价提醒与 AI 智能筛选。
- 交付目标：普通用户双击安装即可使用。
- 支持系统：Windows x64、Apple Silicon Mac（arm64）。
- 安装包名称不得包含版本数字：
  - `TraceSeek-AI-Setup.exe`
  - `TraceSeek-AI.dmg`

## 品牌和界面要求

- 使用黑色、深灰色和暖金黄色的高级配色。
- 软件图标使用项目中的白底黄黑四块图标。
- 图标源文件：`frontend/public/brand-icon.png` 和 `packaging/appicon.png`。
- Windows、Mac、软件左上角及登录页均使用同一图标。
- 普通用户界面不得暴露 SMTP、LLM URL、API Token、模型参数、反爬地址等技术配置。

## 登录规则

- 未登录时不能使用推荐、收藏、条件、运行等任何业务功能。
- 当前方案为软件内部显示闲鱼官方二维码，不弹出 Google Chrome 窗口。
- 用户使用手机闲鱼 App 扫码并确认。
- 退出登录仅删除登录状态，收藏、推荐记录和条件设置继续保存在本机。
- 再次登录后恢复原有数据。
- 退出后不能被后台任务自动重新登录。

## 已完成的主要功能

- 登录强制校验和退出登录逻辑。
- 用户数据与登录状态分离保存。
- 黑灰金色高级 UI。
- 白底品牌图标及 Windows/Mac 图标资源。
- 推荐页去除无用的顶部统计卡片。
- `仅 LLM 通过` 改为 `AI精选`，`全部` 改为 `所有候选`。
- 去除重复的一键 AI 审核按钮。
- 增加 `使用AI智能筛选` 开关。
- 今日降价数量移动到待审推荐旁边。
- 降价商品以绿色背景突出显示。
- 增加按最新、价格升序、价格降序排序。
- 设置页简化为普通用户可理解的选项。
- 默认推荐刷新 15 分钟，收藏刷新 10 分钟。
- 已修复 `/brand-icon.png` 在安装包内被错误返回为主页文件的问题。
- 根目录保留 MIT `LICENSE`，打包配置也会附带该许可证。

## 当前登录实现

- 后端文件：`src/xianyu_crawler/web/login_runner.py`
- 前端文件：`frontend/src/App.jsx`
- 登录接口：
  - `POST /api/login/start`
  - `GET /api/login/status`
  - `POST /api/login/logout`
- 后台 Playwright Chromium 仅用于获取二维码和执行监控，不显示浏览器窗口。
- 用户不需要另外安装 Chrome，也不依赖 Google 在线服务。

## 后续可选功能

以下内容目前只有界面或预留逻辑，正式销售前必须继续开发：

1. 用户账户系统（如后续需要独立会员授权）。
2. 帮助用户配置 AI 接口的人工服务流程。
3. 自动检查更新。
4. Mac 开发者签名和公证；Windows 代码签名。
5. 多台不同系统电脑的完整测试。

## 数据与安全约束

- 不得把任何用户的 API 密钥或 SMTP 密码写入安装包。
- 每名用户的数据必须隔离。
- 客户端只能访问当前用户自己的本机记录。
- 正式发布前需要隐私政策、用户协议和风险提示。
- 不得移除原项目 MIT 许可证及其版权声明。

## 本地源码结构

- `frontend/`：React 前端。
- `src/xianyu_crawler/`：Python 后端、抓取、登录和桌面启动器。
- `packaging/`：Windows/Mac 图标和安装包构建脚本。
- `.github/workflows/release.yml`：GitHub Actions 双平台构建。
- `tests/`：自动化测试。
- `LICENSE`：必须随发行版本保留的 MIT 许可证。

## GitHub 更新和构建流程

1. 将本包源码覆盖到原 GitHub 本地仓库，但不要删除仓库本身。
2. 在 GitHub Desktop 中提交并 Push 到 `main`。
3. GitHub 项目进入 `Actions` → `release` → `Run workflow`。
4. 使用未出现过的新版本号，例如 `1.5.0`。
5. 构建成功后下载 Windows 和 Mac artifacts。
6. 安装并测试扫码登录、退出、再次登录、推荐、收藏、排序和图标。

## 验收清单

- [ ] 软件内所有位置正确显示白底品牌图标。
- [ ] 点击扫码登录后数秒内出现二维码或明确错误信息。
- [ ] 手机确认后自动进入主页。
- [ ] 未登录无法访问任何业务功能。
- [ ] 退出后立即回到初始化登录页。
- [ ] 退出后不会自动重新登录。
- [ ] 再登录后收藏和条件仍在。
- [ ] 推荐、收藏及降价刷新正常。
- [ ] AI 开关和排序可用。
- [ ] Windows 安装和卸载正常。
- [ ] Mac DMG 拖入应用程序后可打开。
- [ ] 安装目录保留 MIT LICENSE。

## 给接手 AI 的要求

- 修改前先阅读 `AGENTS.md`。
- 保留现有品牌、登录规则和数据规则。
- 当前版本不提供支付、充值、额度或 API 中转；AI 使用用户自备接口。
- 每次修改后至少完成 Python 语法检查、前端 JSX/构建检查和压缩包完整性检查。
- 对不懂编程的用户使用一步一步的中文说明。
